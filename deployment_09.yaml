apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
  annotations:
    kubernetes.io/change-cause: "Deployment created for myapp with persistent volume"
    description: "This deployment uses a persistent volume for data storage"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: nginx:1.25.0
        env:
        - name: Prodenv
          valueFrom:
            configMapKeyRef:
              name: myconfig-v3
              key: Age
        
        - name: Testenv
          valueFrom:
            configMapKeyRef:
              name: myconfig-v3
              key: Country
        
        - name: Devenv
          valueFrom:
            configMapKeyRef:
              name: myconfig-v3
              key: Version

        resources:
          limits:
            memory: "128Mi"
            cpu: "250m"
        ports:
        - containerPort: 80

...

apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: LoadBalancer
  selector:
    app: myapp
  ports:
  - port: 8080
    targetPort: 80